# ── Node / pnpm ──
node_modules/
.pnpm-store/

# ── Next.js (legacy apps/web build cache; FE removed 2026-06-15, ignore for safety) ──
.next/
out/
next-env.d.ts

# ── Build output ──
dist/
build/
*.tsbuildinfo

# ── Python ──
__pycache__/
*.py[cod]
.venv/
venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/

# ── Env & secrets ──
.env
.env.*
!.env.example

# ── Data cache (raw Databento chains + decoded/derived case-study artefacts) ──
/data/raw/
/data/case_study/
/data/cache/

# ── Dashboard derived snapshot assets (regenerable from engine) ──
apps/dashboard/public/data/

# ── Dashboard debug/screenshot captures (scratch, not history) ──
apps/dashboard/arc-*.png

# ── Analysis harness outputs (regenerable from data) ──
/analysis/charts/
/analysis/*.txt

# ── Logs & temp / scratch files ──
*.log
*.tmp
*.bak
*.orig
tmp_*
*.tsbuildinfo

# ── Advisor scratch brief (rewritten raw per-prompt, not history) ──
.kilo/advisor/

# ── Generated docs artifact (17MB pandoc export of 04-engine.md; regenerable) ──
docs/04-engine.html

# ── OS / editor ──
.DS_Store
Thumbs.db
.idea/
.vscode/

# Landing build artifacts
apps/landing/node_modules/
apps/landing/dist/
apps/landing/.astro/
