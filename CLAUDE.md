# CLAUDE.md

This repository's canonical AI-agent instructions now live in **[`AGENTS.md`](AGENTS.md)**.

Claude Code: read `AGENTS.md` first, then the human documentation in
[`docs/`](docs/README.md). Start with:

- `AGENTS.md` — agent operating manual (golden rules, verification, gap map).
- `docs/02-locked-contract.md` — the LOCKED CONTRACT (do not change without approval).
- `docs/04-engine.md` — the compute core.
- `docs/08-status-and-gaps.md` — what is real vs. stubbed vs. naive (your task map).

Everything that used to be in this file has been folded into `AGENTS.md` and `docs/`.
